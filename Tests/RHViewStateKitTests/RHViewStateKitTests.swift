    import XCTest
    @testable import RHViewStateKit

    final class RHViewStateKitTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(RHViewStateKit().text, "Hello, World!")
        }
    }
